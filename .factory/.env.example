# adFactory external-resource credentials — EXAMPLE FILE (placeholders only).
# Copy this file to .factory/.env and fill in the real values.
# .factory/.env is gitignored and must NEVER be committed.

# GitHub (git role: branches/PRs — and PMO role: issues)
# Personal access token with repo + issues scopes for kayalimre/Employee-Management-Fullstack-App
GITHUB_TOKEN=ghp_your_token_here
